#include<stdio.h>
#define MAX_BITS 5000
// long long int fun(long long int x){
//     if(x == 0 || x == 1)return 1;
//     return fun(x-2) + fun(x-1);
// }
struct BigInt{
    int bits[MAX_BITS];
};
struct BigInt add(struct BigInt a, struct BigInt b)
{
    
};
struct BigInt init(char s[]){
    for(int i = 0; s[i] != '\0'; i++){
        
    }
}
int main(){
    char 
    scanf("", &a);
    // printf("%ld", fun(a));
    return 0;
}